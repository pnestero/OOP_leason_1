def test_product(tests1_product):
    assert tests1_product.name == "Банан"
    assert tests1_product.description == "Чтобы сесть"
    assert tests1_product.price == 123.12
    assert tests1_product.quantity == 2

